interface:
  display_name: "Humanize v2"
  short_description: "把中文 AI 文案改得更自然、更像真人"
  default_prompt: "Use $humanize-v2 to rewrite Chinese AI-sounding copy into natural, human communication while preserving the key facts."

policy:
  allow_implicit_invocation: true
